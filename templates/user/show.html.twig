{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <a href="{{ path('app_home') }}" class="btn btn-dark">Acceuil</a>
	<a href="{{ path('app_user') }}" class="btn btn-dark">Profile</a>
	<a href="{{ path('app_quizz') }}" class="btn btn-dark">Quizz</a>
	<a href="{{ path('app_logout') }}" class="btn btn-dark">Logout</a>
    <h1>Utilisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ subscribe.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ subscribe.password }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ subscribe.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_edit', {'id': subscribe.id}) }}">Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
