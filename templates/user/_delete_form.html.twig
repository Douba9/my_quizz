<form method="post" action="{{ path('app_admin_delete', {'id': subscribe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this user?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscribe.id) }}">
    <button class="btn">Supprimer</button>
</form>
