{% extends 'base.html.twig' %}

{% block title %}Subscribe{% endblock %}

{% block body %}
    <h1>Subscribe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subscribe.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ subscribe.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ subscribe.password }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ subscribe.email }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ subscribe.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ subscribe.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ subscribe.roles ? subscribe.roles|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}">back to list</a>

    <a href="{{ path('app_admin_edit', {'id': subscribe.id}) }}">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
